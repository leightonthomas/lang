# Programming language
Implementation of a programming language as a learning exercise

* Private by default, opt-in public/protected
* All functions/methods/properties have to have a defined type
  * Variables _can_ but don't have to
* "Namespace" based on directory structure; define root namespace on project creation and pass to compiler

## TODO
